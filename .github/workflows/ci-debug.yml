name: CI (Debug)
on: [push, pull_request]

jobs:
  build:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v6
      with:
        submodules: true

    - uses: actions/setup-dotnet@v5
      with:
        dotnet-version: '10.0.x'

    - name: Restore dependencies
      run: dotnet restore

    - name: Build
      run: dotnet build --no-restore

    - name: Publish
      run: dotnet publish -p:CommitHash=${{ github.sha }} -p:CommitRef=${{ github.ref_type }}/${{ github.ref_name }} -p:PublishDir=bin\Debug\net10.0-windows\publish\win-x64\ -c Debug /p:PublishProfile=Publish-x64

    - name: Upload Artifact
      uses: actions/upload-artifact@v7
      with:
        name: Bloxstrap (Debug) (${{ github.sha }})
        path: .\Bloxstrap\bin\Debug\net10.0-windows\publish\win-x64\*
